#include <stdio.h>

int main(){
    int a, b, count = 0;
    printf("Give numbers: ");
    scanf("%d", &a);

    printf("Enter number: ");
    scanf("%d", &b);

    for(int i = a; i <= b; i++){
        if(i % 4 == 0) {
          
        }
    }

    printf("Total %d ", count);
}